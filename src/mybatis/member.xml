<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="member">
	<select id ="selectAll">
		SELECT *
		FROM
			js_member
	</select>
	
	<select id="login" resultType="org.shoppingMall.vo.Member" parameterType="java.util.Map">
		SELECT * FROM js_member
		WHERE id=#{id} and password=#{password}
	</select>
	
	<insert id="register" parameterType="org.shoppingMall.vo.Member" useGeneratedKeys="false">
	INSERT INTO js_member 
	VALUES 
	(member_idx_seq.nextval,#{id},#{password},#{name},#{nickname},#{email},#{phone},#{c_code},#{address},#{birth})
	<selectKey keyProperty="idx" resultType="long" order="AFTER">
			SELECT member_idx_seq.currval 
			FROM dual
		</selectKey>	
	</insert>
	<select id="findId" resultType="org.shoppingMall.vo.Member" parameterType="java.util.Map">
		SELECT * FROM js_member
		WHERE name=#{name} and email=#{email}
	</select>
	<select id="findPwd" resultType="org.shoppingMall.vo.Member" parameterType="java.util.Map">
		SELECT * FROM js_member
		WHERE id=#{id} and email=#{email}
	</select>
	
</mapper>
 
 